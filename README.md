# fake-newsheadline-genration
  A simple Python project that generates random fake news headlines using predefined lists of subjects, actions, and places. It uses random.choice() to create fun and dynamic headlines and runs interactively until the user chooses to stop. 
